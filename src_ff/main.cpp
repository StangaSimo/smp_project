#include <ff/ff.hpp>
#include <iostream>

int main(int argc, char *argv[])
{
    uint64_t N = 512; /* size of M (NxN) */
    std::vector<int> M(N * N, -1); /* all element -1 */
    std::cout << "hello\n";
}